name: Quickstart Smoke Test

on:
  workflow_dispatch:  # Manual only — does not run automatically

permissions:
  contents: read

jobs:
  quickstart:
    name: Quickstart Bootstrap & Smoke Tests
    runs-on: ubuntu-latest

    # MySQL service commented out - smoke tests should not require database setup
    # services:
    #   mysql:
    #     image: mariadb:10.6
    #     env:
    #       MYSQL_DATABASE: testing
    #       MYSQL_ROOT_PASSWORD: root
    #     ports:
    #       - 3306:3306
    #     options: >-
    #       --health-cmd="mysqladmin ping"
    #       --health-interval=10s
    #       --health-timeout=5s
    #       --health-retries=5

    env:
      # DB_CONNECTION: mysql
      # DB_DATABASE: testing
      # DB_USERNAME: root
      # DB_PASSWORD: root
      # DB_HOST: 127.0.0.1
      APP_ENV: testing

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup PHP with Composer
        uses: ./.github/actions/setup-php-composer
        with:
          php-version: '8.4'
          composer-flags: '--prefer-dist --no-interaction'

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'yarn'

      - name: Install JS dependencies
        run: yarn install --frozen-lockfile

      - name: Build frontend assets
        run: yarn build

      # CodeIgniter 3 does not use Laravel artisan commands
      # Smoke tests not yet configured for this project
      - name: Verify build
        run: |
          echo "Frontend assets built successfully"
          echo "CodeIgniter 3 project - Laravel artisan commands not applicable"
          ls -la assets/

